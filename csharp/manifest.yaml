# sufficient for dotnet apps
builderImages:
  pack: paketobuildpacks/builder-jammy-base:latest

# include for now? idk if can remove
buildpacks:
  - paketo-buildpacks/dotnet-core:latest

# these are the same as defaults
healthEndpoints:
  liveness: /health/liveness
  readiness: /health/readiness